{
  "name": "esa-edge-probe",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Alibaba Cloud ESA Pages edge HTTP probe tool.",
  "scripts": {
    "build": "node -e \"console.log('No build step required. Static assets are in public.')\"",
    "check": "node --check src/index.js"
  }
}
